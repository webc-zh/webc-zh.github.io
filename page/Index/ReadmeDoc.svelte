<script>
import codeUrl, { GIT_HOME } from "~/sh/example/conf/url/CODE.js";
import Md from "~/page/lib/Md.svelte";
import Github from "~/page/lib/MdGithub.svelte";

let { name, readme } = $props();

const url = $derived(name == "WebC" ? GIT_HOME : codeUrl("com/" + name));
</script>

<template lang="pug">
v-scroll
  b
    article
      b
        h1 {name}
        Github(url={ url })
      Md(readme={ readme })
</template>

<style lang="stylus">
@import '~/styl/var.styl'

v-scroll
  flex 1
  min-width 0
  min-height 0

  &::part(scroll)
    box-sizing border-box
    overflow-x hidden

  > b
    display block
    line-height 1.7
    color #333336
    padding var(--gap) var(--gap) var(--gap) 0
    box-sizing border-box

    @media $mobile
      padding var(--gap) 0

    article
      padding 0 0 var(--gap)
      box-sizing border-box
      max-width 800px
      margin 0 auto

      > b
        display flex
        align-items center
        justify-content space-between
        gap 16px
        margin-bottom var(--gap)

        @media $mobile
          align-items flex-start

        :global(a)
          @media $mobile
            margin-right 44px

        h1
          font-size 28px
          font-weight 700
          color #1d1d1f
          line-height 1.2
          margin 0

          @media $mobile
            font-size 22px

    @media $mobile
      font-size 14px

      article
        padding 0 20px var(--gap)
</style>